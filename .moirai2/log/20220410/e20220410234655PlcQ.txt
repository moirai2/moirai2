######################################## e20220410234655PlcQ ########################################
https://moirai2.github.io/schema/daemon/execute	completed
https://moirai2.github.io/schema/daemon/processtime	0
https://moirai2.github.io/schema/daemon/timecompleted	1649602016
https://moirai2.github.io/schema/daemon/timeended	1649602016
https://moirai2.github.io/schema/daemon/timestarted	1649602016
test/test.sh#output	test/uniq.txt
######################################## status ########################################
registered	1649602016
start	1649602016
end	1649602016
completed	1649602016
######################################## insert ########################################
idA	output	test/uniq.txt
######################################## bash ########################################
#!/bin/sh
export PATH=/Users/ah3q/Sites/moirai2:/Users/ah3q/Sites/moirai2/bin:/Users/ah3q/Sites/moirai2/.moirai2/bin:$PATH
cmdurl="test/test.sh"
execid="e20220410234655PlcQ"
workdir=".moirai2/e20220410234655PlcQ"
rootdir="/Users/ah3q/Sites/moirai2"
tmpdir=".moirai2/e20220410234655PlcQ/tmp"
id="idA"
input="test/input.txt"
output=$tmpdir/output.txt
########## init ##########
cd $rootdir
touch $workdir/status.txt
touch $workdir/log.txt
function status() { echo "$1	"`date +%s` >> $workdir/status.txt ; }
function record() { echo "$1	$2" >> $workdir/log.txt ; }
mkdir -p /tmp/$execid
ln -s /tmp/$execid $tmpdir
status start
########## command ##########
sort $input | uniq -c > $output
#############################
status end
mv $output test/uniq.txt
output=test/uniq.txt
if [[ "$(declare -p output)" =~ "declare -a" ]]; then
for out in ${output[@]} ; do
record "$cmdurl#output" "$out"
echo "insert $id->output->$out"
done
else
record "$cmdurl#output" "$output"
echo "insert $id->output->$output"
fi
rm $workdir/tmp
if [ -z "$(ls -A /tmp/$execid)" ]; then
rmdir /tmp/$execid
else
mv /tmp/$execid $workdir/tmp
fi
status=""
if [ -z "$status" ]; then
if [ -s $workdir/stderr.txt ]; then
status=error
fi
fi
if [ -z "$status" ]; then
status=completed
fi
status $status
sleep 1
touch $workdir/status.txt
